import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ngx-socket-io',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
